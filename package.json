{
  "name": "js-oop-todo-list",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:tailwind": "tailwindcss -i ./css/input.css -o ./css/output/tailwind.css",
    "build:sass": "sass ./scss/custom.scss ./css/output/custom.css",
    "build": "npm-run-all build:tailwind build:sass",
    "watch": "watch \"npm run build\" ./ --ignoreDirectoryPattern=/output/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.55.0",
    "tailwindcss": "^3.2.1",
    "watch": "^1.0.2"
  }
}
